import 'package:custom_dropdown/sign_up_screen.dart';
import 'package:custom_dropdown/sliver_demo1.dart';
import 'package:custom_dropdown/sliverapp_demo1.dart';
import 'package:custom_dropdown/sliverdemo1.dart';
import 'package:custom_dropdown/textfield_example1.dart';
import 'package:flutter/material.dart';
import 'my_ui_design.dart';
import 'sliverappdemo1.dart';


void main() {
  //runApp(MyApp());
  //runApp(Example());
 /* runApp(MaterialApp(
      debugShowCheckedModeBanner: false,
      home: MyCustomSignUpPage()));*/
  //runApp(CardDemo());
  //runApp(MyTabBarUIDemo1());
  //runApp(SliverTabBarDemo1());
  //runApp(SliverAppBarDemo2());
  //runApp(SliverAppDemo1());
  //runApp(MySliverDemoExample1());
  //runApp(SliverDemo1());
  //runApp(VerticleExample());
  runApp(MyCustomSignUpPage());
  //runApp(MyCountryCodePicker());
  //runApp(MyCountryCodeDemo());
  //runApp(RadioDemo1());
  //runApp(RadioValueDemo1());
  //runApp(MyRadioDemo1());
  //runApp(MyRadioDemo2());
  //runApp(MyCalendar());
  //runApp(TextFieldExample1());
  //runApp(CountryCodeExample());
  //runApp(CalendarExample9());
  //runApp(CountryCode());
  //runApp(PhoneNumbeWithCountryCode());
}

/*class MyApp extends StatelessWidget {
  // This widget is the root of your application.
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      debugShowCheckedModeBanner: false,
      title: 'Flutter Demo',
      theme: ThemeData(
        // This is the theme of your application.
        //
        // Try running your application with "flutter run". You'll see the
        // application has a blue toolbar. Then, without quitting the app, try
        // changing the primarySwatch below to Colors.green and then invoke
        // "hot reload" (press "r" in the console where you ran "flutter run",
        // or simply save your changes to "hot reload" in a Flutter IDE).
        // Notice that the counter didn't reset back to zero; the application
        // is not restarted.
        primarySwatch: Colors.blue,
      ),
      home:  DropDown(),
    );
  }
}*/